{
  "name": "api-hono",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "wrangler build"
  },
  "dependencies": {
    "hono": "^4.8.3"
  },
  "devDependencies": {
    "@types/bun": "latest"
  }
}
